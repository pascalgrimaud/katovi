entity Message {
    date Instant
    value String
}

entity Topic {
    name String
    active Boolean
    keyDeserializer String
    valueDeserializer String
    groupId String
    offset String
}

relationship ManyToOne {
    Message{topic(name) required} to Topic
}

service * with serviceClass
dto * with mapstruct
paginate * with infinite-scroll
